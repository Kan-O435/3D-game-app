// Where the walls are (metres). The machine stands at the origin; the back wall
// is a metre or so behind it, the side walls hold the order sheet (left) and the
// exchange counter (right). Shared by Room, Props and the camera stations.
export const ROOM = { minX: -3.4, maxX: 3.4, minZ: -1.5, maxZ: 4.5, height: 3.2 } as const
